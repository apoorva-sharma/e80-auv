todo

make sensor init's return success status

failsafes (e.g., no sd card, no sensor, etc.)

write and test analog pin reader class SensorPin.h

write async logger (put control/measurement in interrupt service routine)

test controller in tank
test controller in pool

